{
	"name": "pigeon",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"lerna": "^6.0.1"
	},
	"version": "1.0.0",
	"description": "sql to email",
	"main": "index.js",
	"scripts": {
		"start": "npx lerna run start --stream",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"handlebars": "^4.7.7",
		"postmark": "^3.0.14"
	}
}
